import styled from '@emotion/styled';

const Tag = styled('span')({
    display: 'inline-block',
    padding: '5px 10px',
    borderRadius: '3px',
    backgroundColor: '#ff5100',
    color: '#fff',
    fontSize: '0.875rem',
});

export default Tag;
